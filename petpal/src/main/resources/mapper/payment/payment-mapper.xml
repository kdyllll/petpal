<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="payment">
	<insert id="insertPayment" parameterType="payment">
		INSERT INTO PAYMENT VALUES(SEQ_PAYMENTNO.NEXTVAL, #{memberNo}, #{name}, #{receiverName}, #{tel}, #{receiverTel}, #{loc}, null, 0, 0, #{totalPrice}, #{payKind}, SYSDATE, DEFAULT, '결제완료', #{email})
		<selectKey keyProperty="paymentNo" resultType="string" order="AFTER">
			SELECT SEQ_PAYMENTNO.CURRVAL FROM DUAL
		</selectKey>
	</insert>
	<insert id="insertPayDetail" parameterType="payDetail">
		INSERT INTO PAYDETAIL VALUES(SEQ_DETAILNO.NEXTVAL, #{paymentNo}, #{stockNo}, #{cnt}, '결제')
	</insert>
</mapper>
